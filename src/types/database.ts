/**
 * Placeholder for Supabase-generated database types.
 *
 * Once the local Supabase project is running, regenerate this file with:
 *
 *   npx supabase gen types typescript --local > src/types/database.ts
 *
 * The generated `Database` type is what `createClient<Database>(...)` consumes
 * in `src/lib/supabase.ts` to give every table query full type safety.
 */
export type Database = {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
    CompositeTypes: Record<string, never>
  }
}
