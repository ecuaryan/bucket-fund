/** Internal auth email suffix for PIN-only members (never shown in UI). */
export const PIN_AUTH_EMAIL_SUFFIX = '@pin.bucketmymoney.internal'

/** Previous suffix — used only for one-time localStorage/session migration docs. */
export const LEGACY_PIN_AUTH_EMAIL_SUFFIX = '@pin.bucketfund.internal'

/** Synthetic admin emails in database tests. */
export const TEST_AUTH_EMAIL_DOMAIN = '@test.bucketmymoney.local'
